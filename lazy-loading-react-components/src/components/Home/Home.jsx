import React from "react";
import { Page } from "..";

const Home = () => (
  <Page heading="Welcome...">
    <p>
      Click some of the other pages to dynamically import the other pages in
      this application
    </p>
  </Page>
);

export default Home;
